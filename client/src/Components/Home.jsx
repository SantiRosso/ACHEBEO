import React from 'react';
import FilterButton from './Buttons/FilterButton';
import NavBar from './NavBar';

const Home = () => {
    return (
        <div>
            <NavBar />
        </div>
    );
};

export default Home;